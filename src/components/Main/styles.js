import styled from 'styled-components'

export const Container = styled.main`
  grid-area: content;
  align-self: start;
  min-height: 100%;
  max-width: 100vw;
  padding: 0 clamp(3rem, 0.7273rem + 7.1023vw, 8rem);
`
